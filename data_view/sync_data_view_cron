# The data view py script is copied from WD and not from `./bin` because the cron task is meant
# to run only on the server, and `../deploy.sh` copies the script into WD of the server.
* /15 * * * docker run -v `pwd`/my.cnf:/dash/my.cnf -v `pwd`/create_download_view_for_sample_data.py:/opt/create_download_view_for_sample_data.py gitlab-registry.internal.sanger.ac.uk/sanger-pathogens/monocle/monocle-dash:<DOCKERTAG> bash -c "PYTHONPATH='/dash' python3 /opt/create_download_view_for_sample_data.py"
