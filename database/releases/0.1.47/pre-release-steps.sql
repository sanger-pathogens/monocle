-- =================================================
-- To be applied before the code release is started.
-- =================================================

-- Remove the current sample set, so we can reload from the new spreadsheets
delete from api_sample;

