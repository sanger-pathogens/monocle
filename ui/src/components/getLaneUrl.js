// import env from "../env";

// const getLaneUrl = (laneId) => {
//   let laneId_encoded = `${encodeURIComponent(`${laneId}`)}`;
//   let lane_url = `${env.DOWNLOAD_ROOT_URL}${laneId_encoded}`;
//   return lane_url;
// };

// export default getLaneUrl;
